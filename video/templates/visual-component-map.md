# 视觉组件映射

本文件必须在 `shot-list.md` 前完成。目标是先为每个 scene 选定可复用视觉组件，再进入具体分镜和 HyperFrames 实现。

## 组件库引用

- 使用的组件库版本：
- 本条视频的视觉母题：
- 需要避免的视觉误区：

## 逐段组件选择

| scene_id | retention_goal | 主组件 | 备用组件 | 为什么选它 | 信息负载 | 制作成本 |
| --- | --- | --- | --- | --- | --- | --- |
| S01 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |
| S02 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |
| S03 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |
| S04 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |
| S05 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |
| S06 |  |  |  |  | 低 / 中 / 高 | 低 / 中 / 高 |

## 案例场景组件

- 承载案例的 scene：
- 使用组件：
- 具体场景证据：
- 为什么不是抽象卡片：

## 高成本组件说明

- 是否使用高成本组件：是 / 否
- 使用原因：
- 降级方案：

## 信息负载预检

- 每个 scene 是否只有 1 个主信息：
- 同屏数字是否不超过 3 组：
- 字幕是否可能遮挡主体：
- 是否有明显视觉状态变化：

## 交给 `shot-list.md` 的指令

- 必须保留的组件：
- 可以替换的组件：
- 关键帧抽检重点：
