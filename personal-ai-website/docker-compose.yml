services:
  wisdom-advisor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: personal-ai-website
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env.runtime
